import 'package:get/get.dart';

/// ShiftController sudah diregister di InitialBinding (fenix: true)
/// Binding ini kosong — hanya diperlukan sebagai formalitas route binding.
class ShiftBinding extends Bindings {
  @override
  void dependencies() {}
}
